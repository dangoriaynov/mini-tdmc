apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mini-tdmc-inventory.fullname" . }}-test-connection"
  labels:
    {{- include "mini-tdmc-inventory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mini-tdmc-inventory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
