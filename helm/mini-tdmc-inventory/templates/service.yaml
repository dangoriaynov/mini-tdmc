apiVersion: v1
kind: Service
metadata:
  name: {{ include "mini-tdmc-inventory.fullname" . }}
  labels:
    {{- include "mini-tdmc-inventory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mini-tdmc-inventory.selectorLabels" . | nindent 4 }}
